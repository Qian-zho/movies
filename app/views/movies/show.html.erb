<%= cl_image_tag @movie.photo.key, height: 300, width: 400, crop: :fill %>
<div class="card-category"
  style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @movie.photo.key, height: 300, crop: :fill %>')"
>
</div>

  <div class="text-start">

<h1><%= @movie.synopsis  %></h1>

<p><%= @movie.synopsis %></p>

 <p>synopsis: <%= @movie.synopsis %> </p>
  </div>

<p><strong>Reviews</strong></p>
<ul class="list-group">
  <% @movie.reviews.each do |review| %>
    <li class="list-group-item"><%= @review.content %></li>
  <% end %>
</ul>
<hr>

  <%= @review.content %>
<%= link_to 'Write a review', new_movie_review_path(@movie) %>
